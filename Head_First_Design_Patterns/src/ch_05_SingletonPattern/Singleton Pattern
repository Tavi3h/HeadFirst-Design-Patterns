单件模式确保一个类只有一个实例，并提供一个全局访问点。

要点：
1、单件模式确保程序中一个类只有一个实例。
2、单件模式也提供访问这个实例的全局点
3、在java中实现单件模式需要私有的构造器，一个静态方法和一个静态变量。
4、确定在性能和资源上的限制，然后小心地选择适当的方案来实现单件，以解决多线程问题（我们必须认定所有的程序都是多线程的）
5、java 5之前的版本不要用双重检查加锁法实现单件模式
6、如果使用了多个类加载器，可能会倒置单件失效而产生多个实例
7、JVM 1.2及之前的版本中，必须建立单件注册表，以免垃圾回收器将单件回收